{
  "type": "object",
  "description": "ThreeJS - Tabel",
  "properties": {
    "type": {
      "type": "string",
      "title": "Type",
      "enum": ["table", "sphere", "helix", "grid"],
      "default": "primary",
      "required": "true"
    }
  }
}
